function login() {
    const username = document.getElementById('username').value;
    const password = document.getElementById('password').value;
    alert('Nome de usuário cadastrado: ' + username + '\nSenha cadastrada: ' + password);
};